---
id: 0x687fa141783756da
title: "Solid Mask Default Decal Sticker"
---

# Solid Mask Default Decal Sticker
(`solid_mask:default:DECAL_STICKER`, `0x687fa141783756da`)

This template is referenced 1 time by [solid_mask:DECAL_STICKER](/docs/Generated/Render%20Templates/base.render_template_database#solid-mask-decal-sticker).
This template uses the following inputs:
 |Name|UI Name|Type|
|---|---|---|
|diffuse_texture|Diffuse|texture|
|bump_normal_texture|Normalmap|texture|
|reflection_texture|MATCAP Texture|texture|
|material_texture|Pattern texture|texture|
|diffuse_layer3_texture|Decal (UV5 and UV6)|texture|
|uv_scale|Sticker Scale x y, Sticker Wear and Tear Influence|vector3|
|uv_offset_rot|Sticker Position Offset x y, Sticker Rotation|vector3|
|tint_color_a|Tint Color A|vector3|
|tint_color_b|Tint Color B|vector3|
|material_amount|Material apply amount|scalar|.

### Technique `deferred_ambient`
### Render States
More info can be found on the [Direct3D Docs](https://learn.microsoft.com/en-us/windows/win32/direct3d9/d3drenderstatetype)
```cpp
enum D3DRENDERSTATETYPE {
	D3DRS_ZENABLE = 1,
	D3DRS_ZFUNC = 4,
	D3DRS_ZWRITEENABLE = 1,
	D3DRS_ALPHABLENDENABLE = 0,
	D3DRS_CULLMODE = 2,
	D3DRS_COLORWRITEENABLE = 15,
	D3DRS_COLORWRITEENABLE1 = 15,
	D3DRS_COLORWRITEENABLE2 = 15,
	D3DRS_STENCILENABLE = 1,
	D3DRS_STENCILFUNC = 8,
	D3DRS_STENCILFAIL = 3,
	D3DRS_STENCILMASK = -1,
	D3DRS_STENCILPASS = 3,
	D3DRS_STENCILREF = 1,
	D3DRS_STENCILWRITEMASK = 255,
	D3DRS_STENCILZFAIL = 1,
	D3DRS_DEPTHBIAS = 0,
	D3DRS_SLOPESCALEDEPTHBIAS = 0,
	D3DRS_FOGENABLE = 0,
	D3DRS_SPECULARENABLE = 0,
	D3DRS_ADAPTIVETESS_X = 0,
	D3DRS_ADAPTIVETESS_Z = 0,
	D3DRS_ADAPTIVETESS_W = 0,
	D3DRS_SRGBWRITEENABLE = 0,
	D3DRS_SEPARATEALPHABLENDENABLE = 0,
	D3DRS_ALPHATESTENABLE = 0,
};
```
### Sampler States
More info can be found on the [Direct3D Docs](https://learn.microsoft.com/en-us/windows/win32/direct3d9/d3dsamplerstatetype)
#### Sampler 0
```cpp
enum D3DSAMPLERSTATETYPE {
	D3DSAMP_MIPMAPLODBIAS = 827606343,
	D3DSAMP_SRGBTEXTURE = 1,
	D3DSAMP_ADDRESSU = 1,
	D3DSAMP_ADDRESSV = 1,
	D3DSAMP_MAGFILTER = 3,
	D3DSAMP_MINFILTER = 3,
	D3DSAMP_MIPFILTER = 3,
};
```
#### Sampler 1
```cpp
enum D3DSAMPLERSTATETYPE {
	D3DSAMP_MIPMAPLODBIAS = 827606343,
	D3DSAMP_SRGBTEXTURE = 0,
	D3DSAMP_ADDRESSU = 1,
	D3DSAMP_ADDRESSV = 1,
	D3DSAMP_MAGFILTER = 3,
	D3DSAMP_MINFILTER = 3,
	D3DSAMP_MIPFILTER = 3,
};
```
#### Sampler 2
```cpp
enum D3DSAMPLERSTATETYPE {
	D3DSAMP_MIPMAPLODBIAS = 827606343,
	D3DSAMP_SRGBTEXTURE = 1,
	D3DSAMP_ADDRESSU = 1,
	D3DSAMP_ADDRESSV = 1,
	D3DSAMP_MAGFILTER = 3,
	D3DSAMP_MINFILTER = 3,
	D3DSAMP_MIPFILTER = 3,
};
```
#### Sampler 3
```cpp
enum D3DSAMPLERSTATETYPE {
	D3DSAMP_MIPMAPLODBIAS = 827606343,
	D3DSAMP_SRGBTEXTURE = 1,
	D3DSAMP_ADDRESSU = 1,
	D3DSAMP_ADDRESSV = 1,
	D3DSAMP_MAGFILTER = 3,
	D3DSAMP_MINFILTER = 3,
	D3DSAMP_MIPFILTER = 3,
};
```
#### Sampler 4
```cpp
enum D3DSAMPLERSTATETYPE {
	D3DSAMP_MIPMAPLODBIAS = 827606343,
	D3DSAMP_SRGBTEXTURE = 1,
	D3DSAMP_ADDRESSU = 3,
	D3DSAMP_ADDRESSV = 3,
	D3DSAMP_MAGFILTER = 2,
	D3DSAMP_MINFILTER = 2,
	D3DSAMP_MIPFILTER = 2,
};
```
### Shaders
#### Vertex Shader
```hlsl
float camera_near_range : register(vs_3_0, c12);
column_major float4x4 proj_matrix : register(vs_3_0, c8);
float3 ref_uv_offset_rot : register(vs_3_0, c14);
float3 ref_uv_scale : register(vs_3_0, c13);
column_major float4x4 view_matrix : register(vs_3_0, c4);
column_major float4x4 world_matrix : register(vs_3_0, c0);
struct VertexMain_Input
{
    float4 position : POSITION;
    float4 normal : NORMAL;
    float4 tangent : TANGENT;
    float4 binormal : BINORMAL;
    float4 texcoord : TEXCOORD;
    float4 texcoord1 : TEXCOORD1;
};

struct VertexMain_Output
{
    float4 position : POSITION;
    float4 texcoord : TEXCOORD;
    float4 texcoord2 : TEXCOORD2;
    float4 texcoord1 : TEXCOORD1;
    float2 texcoord3 : TEXCOORD3;
    float3 texcoord4 : TEXCOORD4;
    float2 texcoord5 : TEXCOORD5;
    float texcoord6 : TEXCOORD6;
};

VertexMain_Output VertexMain(VertexMain_Input i)
{
    VertexMain_Output o;
    float4 temp0, temp1;
    float2 temp2;
    // def c15, 2, -1, 0.5, -0.5
    // def c16, 0.15915494, 0.5, 6.2831855, -3.1415927
    // def c17, 1.000001, 0, 0, 0
    // dcl_position v0
    // dcl_normal v1
    // dcl_tangent v2
    // dcl_binormal v3
    // dcl_texcoord v4
    // dcl_texcoord1 v5
    // dcl_position o0
    // dcl_texcoord o1
    // dcl_texcoord2 o2
    // dcl_texcoord1 o3
    // dcl_texcoord3 o4.xy
    // dcl_texcoord4 o5.xyz
    // dcl_texcoord5 o6.xy
    // dcl_texcoord6 o7.x
    // dp4 r0.w, v0, c3
    temp0.w = dot(i.position, (world_matrix._m03_m13_m23_m33));
    // dp4 r0.x, v0, c0
    temp0.x = dot(i.position, (world_matrix._m00_m10_m20_m30));
    // dp4 r0.y, v0, c1
    temp0.y = dot(i.position, (world_matrix._m01_m11_m21_m31));
    // dp4 r0.z, v0, c2
    temp0.z = dot(i.position, (world_matrix._m02_m12_m22_m32));
    // dp4 r1.x, r0, c4
    temp1.x = dot(temp0, (view_matrix._m00_m10_m20_m30));
    // dp4 r1.y, r0, c5
    temp1.y = dot(temp0, (view_matrix._m01_m11_m21_m31));
    // dp4 r1.z, r0, c6
    temp1.z = dot(temp0, (view_matrix._m02_m12_m22_m32));
    // dp4 r1.w, r0, c7
    temp1.w = dot(temp0, (view_matrix._m03_m13_m23_m33));
    // mov o5.xyz, r0
    o.texcoord4 = temp0;
    // dp4 o0.x, r1, c8
    o.position.x = dot(temp1, (proj_matrix._m00_m10_m20_m30));
    // dp4 o0.y, r1, c9
    o.position.y = dot(temp1, (proj_matrix._m01_m11_m21_m31));
    // dp4 o0.w, r1, c11
    o.position.w = dot(temp1, (proj_matrix._m03_m13_m23_m33));
    // dp4 r0.x, r1, c10
    temp0.x = dot(temp1, (proj_matrix._m02_m12_m22_m32));
    // mad r0.yzw, v2.xxyz, c15.x, c15.y
    temp0.yzw = i.tangent.xyz * float3(2, 2, 2) + float3(-1, -1, -1);
    // dp3 o3.x, r0.yzww, c0
    o.texcoord1.x = dot(temp0.yzw, (world_matrix._m00_m10_m20_m30).xyz);
    // dp3 o2.x, r0.yzww, c1
    o.texcoord2.x = dot(temp0.yzw, (world_matrix._m01_m11_m21_m31).xyz);
    // dp3 o1.z, r0.yzww, c2
    o.texcoord.z = dot(temp0.yzw, (world_matrix._m02_m12_m22_m32).xyz);
    // mad r0.yzw, v3.xxyz, c15.x, c15.y
    temp0.yzw = i.binormal.xyz * float3(2, 2, 2) + float3(-1, -1, -1);
    // dp3 o3.y, -r0.yzww, c0
    o.texcoord1.y = dot(-temp0.yzw, (world_matrix._m00_m10_m20_m30).xyz);
    // dp3 o2.y, -r0.yzww, c1
    o.texcoord2.y = dot(-temp0.yzw, (world_matrix._m01_m11_m21_m31).xyz);
    // dp3 o1.w, -r0.yzww, c2
    o.texcoord.w = dot(-temp0.yzw, (world_matrix._m02_m12_m22_m32).xyz);
    // mad r0.yzw, v1.xxyz, c15.x, c15.y
    temp0.yzw = i.normal.xyz * float3(2, 2, 2) + float3(-1, -1, -1);
    // dp3 o3.z, r0.yzww, c0
    o.texcoord1.z = dot(temp0.yzw, (world_matrix._m00_m10_m20_m30).xyz);
    // dp3 o2.z, r0.yzww, c1
    o.texcoord2.z = dot(temp0.yzw, (world_matrix._m01_m11_m21_m31).xyz);
    // dp3 o2.w, r0.yzww, c2
    o.texcoord2.w = dot(temp0.yzw, (world_matrix._m02_m12_m22_m32).xyz);
    // mov r0.y, c15.y
    temp0.y = float1(-1);
    // add r0.yzw, r0.y, c13.xxyy
    temp0.yzw = temp0.yyy + ref_uv_scale.xyy;
    // mul r0.yzw, r0, c15.z
    temp0.yzw = temp0.yzw * float3(0.5, 0.5, 0.5);
    // add r1.xyz, c14.xyyw, v5.xyyw
    temp1.xyz = ref_uv_offset_rot.xyy + i.texcoord1.xyy;
    // mad r0.yzw, r1.xxyz, c13.xxyy, -r0
    temp0.yzw = temp1.xyz * ref_uv_scale.xyy + -temp0.yzw;
    // add r0.yzw, r0, c15.w
    temp0.yzw = temp0.yzw + float3(-0.5, -0.5, -0.5);
    // mov r1.z, c14.z
    temp1.z = ref_uv_offset_rot.z;
    // mad r1.x, r1.z, c16.x, c16.y
    temp1.x = temp1.z * float1(0.15915494) + float1(0.5);
    // frc r1.x, r1.x
    temp1.x = frac(temp1.x);
    // mad r1.x, r1.x, c16.z, c16.w
    temp1.x = temp1.x * float1(6.2831855) + float1(-3.1415927);
    // sincos r2.xy, r1.x
    temp2.xy = float2(cos(temp1.x), sin(temp1.x));
    // mul r1.xyz, r0.yzww, r2.xyxw
    temp1.xyz = temp0.yzw * temp2.xyx;
    // mad r2.y, r0.y, -r2.y, r1.z
    temp2.y = temp0.y * -temp2.y + temp1.z;
    // add r2.x, r1.y, r1.x
    temp2.x = temp1.y + temp1.x;
    // add o6.xy, r2, c15.z
    o.texcoord5 = temp2 + float4(0.5, 0.5, 0.5, 0.5);
    // add r0.y, c17.x, -v5.x
    temp0.y = float1(1.000001) + -i.texcoord1.x;
    // slt r0.z, -r0.y, r0.y
    temp0.z = (-temp0.y < temp0.y) ? 1 : 0;
    // slt r0.y, r0.y, -r0.y
    temp0.y = (temp0.y < -temp0.y) ? 1 : 0;
    // add r0.y, -r0.y, r0.z
    temp0.y = -temp0.y + temp0.z;
    // max o7.x, r0.y, c17.y
    o.texcoord6 = max(temp0.y, float4(0, 0, 0, 0));
    // add o3.w, r0.x, c12.x
    o.texcoord1.w = temp0.x + camera_near_range.x;
    // mov o0.z, r0.x
    o.position.z = temp0.x;
    // mov o1.xy, v4
    o.texcoord.xy = i.texcoord.xy;
    // mov o4.xy, v5
    o.texcoord3 = i.texcoord1;
    // 

    return o;
}
```
Decompiled by DXDecompiler.
#### Pixel Shader
```hlsl
sampler2D bump_normal_texture : register(ps_3_0, s1);
column_major float4x4 camera_world_matrix : register(ps_3_0, c0);
sampler2D diffuse_layer3_texture : register(ps_3_0, s4);
sampler2D diffuse_texture : register(ps_3_0, s0);
sampler2D material_texture : register(ps_3_0, s3);
float ref_material_amount : register(ps_3_0, c6);
float3 ref_tint_color_a : register(ps_3_0, c4);
float3 ref_tint_color_b : register(ps_3_0, c5);
float3 ref_uv_scale : register(ps_3_0, c3);
sampler2D reflection_texture : register(ps_3_0, s2);
struct PixelMain_Input
{
    float4 texcoord : TEXCOORD;
    float4 texcoord2 : TEXCOORD2;
    float4 texcoord1 : TEXCOORD1;
    float2 texcoord3 : TEXCOORD3;
    float2 texcoord5 : TEXCOORD5;
    float texcoord6 : TEXCOORD6;
};

struct PixelMain_Output
{
    float4 color1 : COLOR1;
    float4 color : COLOR;
    float4 color2 : COLOR2;
};

PixelMain_Output PixelMain(PixelMain_Input i)
{
    PixelMain_Output o;
    float4 temp0, temp1, temp3;
    float3 temp2;
    // def c7, -0.5, 1, 0.5, 6
    // def c8, 1, -1, 0.5, 0
    // dcl_texcoord v0
    // dcl_texcoord2 v1
    // dcl_texcoord1 v2
    // dcl_texcoord3 v3.xy
    // dcl_texcoord5 v4.xy
    // dcl_texcoord6 v5.x
    // dcl_2d s0
    // dcl_2d s1
    // dcl_2d s2
    // dcl_2d s3
    // dcl_2d s4
    // mov r0.xy, v0.zwzw
    temp0.xy = i.texcoord.zw;
    // mov r0.z, v1.w
    temp0.z = i.texcoord2.w;
    // texld_pp r1, v0, s1
    temp1 = /* not implemented _pp modifier */ tex2D(bump_normal_texture, i.texcoord.xy);
    // add_pp r1.xy, r1.wyzw, c7.x
    temp1.xy = /* not implemented _pp modifier */ temp1.wy + float2(-0.5, -0.5);
    // add_pp r1.xy, r1, r1
    temp1.xy = /* not implemented _pp modifier */ temp1.xy + temp1.xy;
    // mul_pp r0.w, r1.x, r1.x
    temp0.w = /* not implemented _pp modifier */ temp1.x * temp1.x;
    // mad_pp r0.w, r1.y, r1.y, -r0.w
    temp0.w = /* not implemented _pp modifier */ temp1.y * temp1.y + -temp0.w;
    // add_pp r0.w, -r0.w, c7.y
    temp0.w = /* not implemented _pp modifier */ -temp0.w + float1(1);
    // rsq_pp r0.w, r0.w
    temp0.w = /* not implemented _pp modifier */ 1 / sqrt(temp0.w);
    // rcp_pp r1.z, r0.w
    temp1.z = /* not implemented _pp modifier */ 1.0f / temp0.w;
    // dp3 r0.z, r1, r0
    temp0.z = dot(temp1.xyz, temp0.xyz);
    // dp3 r0.x, r1, v2
    temp0.x = dot(temp1.xyz, i.texcoord1.xyz);
    // dp3 r0.y, r1, v1
    temp0.y = dot(temp1.xyz, i.texcoord2.xyz);
    // nrm_pp r1.xyz, r0
    temp1.xyz = /* not implemented _pp modifier */ normalize(temp0.xyz).xyz;
    // mov r0.x, c0.y
    temp0.x = (camera_world_matrix._m00_m10_m20_m30).y;
    // mov r0.y, c1.y
    temp0.y = (camera_world_matrix._m01_m11_m21_m31).y;
    // mov r0.z, c2.y
    temp0.z = (camera_world_matrix._m02_m12_m22_m32).y;
    // dp3 r0.x, r0, r1
    temp0.x = dot(temp0.xyz, temp1.xyz);
    // mov r2.x, c0.z
    temp2.x = (camera_world_matrix._m00_m10_m20_m30).z;
    // mov r2.y, c1.z
    temp2.y = (camera_world_matrix._m01_m11_m21_m31).z;
    // mov r2.z, c2.z
    temp2.z = (camera_world_matrix._m02_m12_m22_m32).z;
    // dp3 r0.y, r2, r1
    temp0.y = dot(temp2.xyz, temp1.xyz);
    // mad_pp oC1.xyz, r1, c7.z, c7.z
    o.color1.xyz = /* not implemented _pp modifier */ temp1.xyz * float3(0.5, 0.5, 0.5) + float3(0.5, 0.5, 0.5);
    // add r0.xy, r0, c7.y
    temp0.xy = temp0.xy + float2(1, 1);
    // mul r0.xy, r0, c7.z
    temp0.xy = temp0.xy * float2(0.5, 0.5);
    // mad r0.xy, r0, c8, c8.zxzw
    temp0.xy = temp0.xy * float2(1, -1) + float2(0.5, 1);
    // mad r0.xy, v0, -c7.w, r0
    temp0.xy = i.texcoord.xy * float2(-6, -6) + temp0.xy;
    // mul r0.zw, c7.w, v0.xyxy
    temp0.zw = float2(6, 6) * i.texcoord.xy;
    // mad r0.xy, c6.x, r0, r0.zwzw
    temp0.xy = ref_material_amount.xx * temp0.xy + temp0.zw;
    // texld r0, r0, s2
    temp0 = tex2D(reflection_texture, temp0.xy);
    // texld r1, v0, s0
    temp1 = tex2D(diffuse_texture, i.texcoord.xy);
    // add r2.x, r1.y, r1.x
    temp2.x = temp1.y + temp1.x;
    // add r2.x, r1.z, r2.x
    temp2.x = temp1.z + temp2.x;
    // mul_sat r2.x, r2.x, c7.z
    temp2.x = saturate(temp2.x * float1(0.5));
    // rsq r2.x, r2.x
    temp2.x = 1 / sqrt(temp2.x);
    // rcp r2.x, r2.x
    temp2.x = 1.0f / temp2.x;
    // mad r0, r0, r2.x, -r1
    temp0 = temp0 * temp2.x + -temp1;
    // texld r3, v3, s3
    temp3 = tex2D(material_texture, i.texcoord3.xy);
    // mul r2.y, r3.z, v5.x
    temp2.y = temp3.z * i.texcoord6.x;
    // mad r0, r2.y, r0, r1
    temp0 = temp2.y * temp0 + temp1;
    // mad r1.xyz, c5, r2.x, -r0
    temp1.xyz = ref_tint_color_b.xyz * temp2.xxx + -temp0.xyz;
    // mad r0.xyz, r3.y, r1, r0
    temp0.xyz = temp3.yyy * temp1.xyz + temp0.xyz;
    // mov oC1.w, r0.w
    o.color1.w = temp0.w;
    // mad r1.xyz, c4, r2.x, -r0
    temp1.xyz = ref_tint_color_a.xyz * temp2.xxx + -temp0.xyz;
    // mad r0.xyz, r3.x, r1, r0
    temp0.xyz = temp3.xxx * temp1.xyz + temp0.xyz;
    // mov r1.x, v5.x
    temp1.x = i.texcoord6.x;
    // mul r1.xy, r1.x, v4
    temp1.xy = temp1.xx * i.texcoord5.xy;
    // texld r1, r1, s4
    temp1 = tex2D(diffuse_layer3_texture, temp1.xy);
    // add r1.xyz, -r0, r1
    temp1.xyz = -temp0.xyz + temp1.xyz;
    // mul r0.w, r1.w, v5.x
    temp0.w = temp1.w * i.texcoord6.x;
    // mul r0.w, r0.w, c3.z
    temp0.w = temp0.w * ref_uv_scale.z;
    // mad oC0.xyz, r0.w, r1, r0
    o.color.xyz = temp0.www * temp1.xyz + temp0.xyz;
    // mov oC0.w, c7.z
    o.color.w = float1(0.5);
    // mov oC2, v2.w
    o.color2 = i.texcoord1.w;
    // 

    return o;
}
```
Decompiled by DXDecompiler.
