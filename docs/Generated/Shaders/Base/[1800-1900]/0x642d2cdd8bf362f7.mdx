---
id: 0x642d2cdd8bf362f7
title: "Opacity Default Ati Cube Environment Mapping Cube Fresnel Diffuse Texture Fps Normalmap Take Global Light"
---

# Opacity Default Ati Cube Environment Mapping Cube Fresnel Diffuse Texture Fps Normalmap Take Global Light
(`opacity:default:ATI:CUBE_ENVIRONMENT_MAPPING:CUBE_FRESNEL:DIFFUSE_TEXTURE:FPS:NORMALMAP:TAKE_GLOBAL_LIGHT`, `0x642d2cdd8bf362f7`)

This template is referenced 1 time by [opacity:CUBE_ENVIRONMENT_MAPPING:CUBE_FRESNEL:DIFFUSE_TEXTURE:FPS:NORMALMAP:TAKE_GLOBAL_LIGHT](/docs/Generated/Render%20Templates/base.render_template_database#opacity-cube-environment-mapping-cube-fresnel-diffuse-texture-fps-normalmap-take-global-light).
This template uses the following inputs:
 |Name|UI Name|Type|
|---|---|---|
|reflection_texture|Reflection Cube|texture|
|opacity_texture|Opacity Texture (UV5)|texture|
|diffuse_texture|Diffuse Texture (UV1)|texture|
|bump_normal_texture|Normal Map (UV2 else UV1)|texture|
|fresnel_settings|Falloff Settings (Falloff curve, Falloff multiplier, Opacity dimmer)|vector3|.

### Technique `transparent_fps`
### Render States
More info can be found on the [Direct3D Docs](https://learn.microsoft.com/en-us/windows/win32/direct3d9/d3drenderstatetype)
```cpp
enum D3DRENDERSTATETYPE {
	D3DRS_ZENABLE = 1,
	D3DRS_ZFUNC = 4,
	D3DRS_ZWRITEENABLE = 0,
	D3DRS_ALPHABLENDENABLE = 1,
	D3DRS_CULLMODE = 2,
	D3DRS_COLORWRITEENABLE = 7,
	D3DRS_COLORWRITEENABLE1 = 15,
	D3DRS_COLORWRITEENABLE2 = 15,
	D3DRS_STENCILENABLE = 0,
	D3DRS_STENCILFUNC = 1,
	D3DRS_STENCILFAIL = 1,
	D3DRS_STENCILMASK = 0,
	D3DRS_STENCILPASS = 1,
	D3DRS_STENCILREF = 0,
	D3DRS_STENCILWRITEMASK = 0,
	D3DRS_STENCILZFAIL = 1,
	D3DRS_DEPTHBIAS = 0,
	D3DRS_SLOPESCALEDEPTHBIAS = 0,
	D3DRS_FOGENABLE = 0,
	D3DRS_SPECULARENABLE = 0,
	D3DRS_ADAPTIVETESS_X = 0,
	D3DRS_ADAPTIVETESS_Z = 0,
	D3DRS_ADAPTIVETESS_W = 0,
	D3DRS_SRGBWRITEENABLE = 0,
	D3DRS_SEPARATEALPHABLENDENABLE = 0,
	D3DRS_ALPHATESTENABLE = 0,
	D3DRS_BLENDOP = 1,
	D3DRS_DESTBLEND = 6,
	D3DRS_SRCBLEND = 5,
};
```
### Sampler States
More info can be found on the [Direct3D Docs](https://learn.microsoft.com/en-us/windows/win32/direct3d9/d3dsamplerstatetype)
#### Sampler 0
```cpp
enum D3DSAMPLERSTATETYPE {
	D3DSAMP_MIPMAPLODBIAS = 827606343,
	D3DSAMP_SRGBTEXTURE = 1,
	D3DSAMP_ADDRESSU = 1,
	D3DSAMP_ADDRESSV = 1,
	D3DSAMP_MAGFILTER = 3,
	D3DSAMP_MINFILTER = 3,
	D3DSAMP_MIPFILTER = 3,
};
```
#### Sampler 1
```cpp
enum D3DSAMPLERSTATETYPE {
	D3DSAMP_MIPMAPLODBIAS = 827606343,
	D3DSAMP_SRGBTEXTURE = 0,
	D3DSAMP_ADDRESSU = 1,
	D3DSAMP_ADDRESSV = 1,
	D3DSAMP_MAGFILTER = 3,
	D3DSAMP_MINFILTER = 3,
	D3DSAMP_MIPFILTER = 3,
};
```
#### Sampler 2
```cpp
enum D3DSAMPLERSTATETYPE {
	D3DSAMP_MIPMAPLODBIAS = 827606343,
	D3DSAMP_SRGBTEXTURE = 0,
	D3DSAMP_ADDRESSU = 3,
	D3DSAMP_ADDRESSV = 3,
	D3DSAMP_MAGFILTER = 2,
	D3DSAMP_MINFILTER = 2,
	D3DSAMP_MIPFILTER = 2,
};
```
#### Sampler 3
```cpp
enum D3DSAMPLERSTATETYPE {
	D3DSAMP_MIPMAPLODBIAS = 827606343,
	D3DSAMP_SRGBTEXTURE = 1,
	D3DSAMP_ADDRESSU = 1,
	D3DSAMP_ADDRESSV = 1,
	D3DSAMP_MAGFILTER = 3,
	D3DSAMP_MINFILTER = 3,
	D3DSAMP_MIPFILTER = 3,
};
```
#### Sampler 4
```cpp
enum D3DSAMPLERSTATETYPE {
	D3DSAMP_MIPMAPLODBIAS = 827606343,
	D3DSAMP_SRGBTEXTURE = 0,
	D3DSAMP_ADDRESSU = 1,
	D3DSAMP_ADDRESSV = 1,
	D3DSAMP_MAGFILTER = 3,
	D3DSAMP_MINFILTER = 3,
	D3DSAMP_MIPFILTER = 3,
};
```
### Shaders
#### Vertex Shader
```hlsl
float camera_near_range : register(vs_3_0, c12);
column_major float4x4 proj_matrix : register(vs_3_0, c8);
column_major float4x4 view_matrix : register(vs_3_0, c4);
column_major float4x4 world_matrix : register(vs_3_0, c0);
struct VertexMain_Input
{
    float4 position : POSITION;
    float4 normal : NORMAL;
    float4 texcoord1 : TEXCOORD1;
    float4 texcoord4 : TEXCOORD4;
    float4 texcoord : TEXCOORD;
    float4 tangent : TANGENT;
    float4 binormal : BINORMAL;
};

struct VertexMain_Output
{
    float4 position : POSITION;
    float4 texcoord5 : TEXCOORD5;
    float2 texcoord1 : TEXCOORD1;
    float3 texcoord4 : TEXCOORD4;
    float4 texcoord : TEXCOORD;
    float3 texcoord2 : TEXCOORD2;
    float3 texcoord3 : TEXCOORD3;
};

VertexMain_Output VertexMain(VertexMain_Input i)
{
    VertexMain_Output o;
    float4 temp0, temp1;
    // def c13, 2, -1, 0, 0
    // dcl_position v0
    // dcl_normal v1
    // dcl_texcoord1 v2
    // dcl_texcoord4 v3
    // dcl_texcoord v4
    // dcl_tangent v5
    // dcl_binormal v6
    // dcl_position o0
    // dcl_texcoord5 o1
    // dcl_texcoord1 o2.xy
    // dcl_texcoord4 o3.xyz
    // dcl_texcoord o4
    // dcl_texcoord2 o5.xyz
    // dcl_texcoord3 o6.xyz
    // dp4 r0.w, v0, c3
    temp0.w = dot(i.position, (world_matrix._m03_m13_m23_m33));
    // dp4 r0.x, v0, c0
    temp0.x = dot(i.position, (world_matrix._m00_m10_m20_m30));
    // dp4 r0.y, v0, c1
    temp0.y = dot(i.position, (world_matrix._m01_m11_m21_m31));
    // dp4 r0.z, v0, c2
    temp0.z = dot(i.position, (world_matrix._m02_m12_m22_m32));
    // dp4 r1.x, r0, c4
    temp1.x = dot(temp0, (view_matrix._m00_m10_m20_m30));
    // dp4 r1.y, r0, c5
    temp1.y = dot(temp0, (view_matrix._m01_m11_m21_m31));
    // dp4 r1.z, r0, c6
    temp1.z = dot(temp0, (view_matrix._m02_m12_m22_m32));
    // dp4 r1.w, r0, c7
    temp1.w = dot(temp0, (view_matrix._m03_m13_m23_m33));
    // mov o1.xyz, r0
    o.texcoord5.xyz = temp0.xyz;
    // dp4 o0.x, r1, c8
    o.position.x = dot(temp1, (proj_matrix._m00_m10_m20_m30));
    // dp4 o0.y, r1, c9
    o.position.y = dot(temp1, (proj_matrix._m01_m11_m21_m31));
    // dp4 o0.w, r1, c11
    o.position.w = dot(temp1, (proj_matrix._m03_m13_m23_m33));
    // dp4 r0.x, r1, c10
    temp0.x = dot(temp1, (proj_matrix._m02_m12_m22_m32));
    // mad r0.yzw, v5.xxyz, c13.x, c13.y
    temp0.yzw = i.tangent.xyz * float3(2, 2, 2) + float3(-1, -1, -1);
    // dp3 o5.x, r0.yzww, c0
    o.texcoord2.x = dot(temp0.yzw, (world_matrix._m00_m10_m20_m30).xyz);
    // dp3 o6.x, r0.yzww, c1
    o.texcoord3.x = dot(temp0.yzw, (world_matrix._m01_m11_m21_m31).xyz);
    // dp3 o3.x, r0.yzww, c2
    o.texcoord4.x = dot(temp0.yzw, (world_matrix._m02_m12_m22_m32).xyz);
    // mad r0.yzw, v6.xxyz, c13.x, c13.y
    temp0.yzw = i.binormal.xyz * float3(2, 2, 2) + float3(-1, -1, -1);
    // dp3 o5.y, -r0.yzww, c0
    o.texcoord2.y = dot(-temp0.yzw, (world_matrix._m00_m10_m20_m30).xyz);
    // dp3 o6.y, -r0.yzww, c1
    o.texcoord3.y = dot(-temp0.yzw, (world_matrix._m01_m11_m21_m31).xyz);
    // dp3 o3.y, -r0.yzww, c2
    o.texcoord4.y = dot(-temp0.yzw, (world_matrix._m02_m12_m22_m32).xyz);
    // mad r0.yzw, v1.xxyz, c13.x, c13.y
    temp0.yzw = i.normal.xyz * float3(2, 2, 2) + float3(-1, -1, -1);
    // dp3 o5.z, r0.yzww, c0
    o.texcoord2.z = dot(temp0.yzw, (world_matrix._m00_m10_m20_m30).xyz);
    // dp3 o6.z, r0.yzww, c1
    o.texcoord3.z = dot(temp0.yzw, (world_matrix._m01_m11_m21_m31).xyz);
    // dp3 o3.z, r0.yzww, c2
    o.texcoord4.z = dot(temp0.yzw, (world_matrix._m02_m12_m22_m32).xyz);
    // add o1.w, r0.x, c12.x
    o.texcoord5.w = temp0.x + camera_near_range.x;
    // mov o0.z, r0.x
    o.position.z = temp0.x;
    // mov o2.xy, v3
    o.texcoord1 = i.texcoord4;
    // mov o4.xy, v4
    o.texcoord.xy = i.texcoord.xy;
    // mov o4.zw, v2.xyxy
    o.texcoord.zw = i.texcoord1.xy;
    // 

    return o;
}
```
Decompiled by DXDecompiler.
#### Pixel Shader
```hlsl
float3 GLOBAL_ambient_color : register(ps_3_0, c18);
float3 GLOBAL_shadow_slice_depths : register(ps_3_0, c16);
sampler2D bump_normal_texture : register(ps_3_0, s4);
column_major float4x4 camera_world_matrix : register(ps_3_0, c12);
sampler2D diffuse_texture : register(ps_3_0, s3);
float3 global_light_col : register(ps_3_0, c19);
float3 global_light_dir : register(ps_3_0, c15);
column_major float4x4 global_shadow_projection[3] : register(ps_3_0, c0);
sampler2D opacity_texture : register(ps_3_0, s1);
float3 ref_fresnel_settings : register(ps_3_0, c17);
samplerCUBE reflection_texture : register(ps_3_0, s0);
sampler2D shadow_depth : register(ps_3_0, s2);
struct PixelMain_Input
{
    float4 texcoord5 : TEXCOORD5;
    float3 texcoord4 : TEXCOORD4;
    float4 texcoord : TEXCOORD;
    float3 texcoord2 : TEXCOORD2;
    float3 texcoord3 : TEXCOORD3;
};

float4 PixelMain(PixelMain_Input i) : COLOR
{
    float4 out_color;
    float4 temp0, temp1, temp3, temp4, temp5, temp6;
    float3 temp2;
    // def c20, -0.5, 1, -1, 0.5773672
    // def c21, 0.9, 511.5, 0.3, 0
    // def c22, 0, 1, 2, 3
    // dcl_texcoord5 v0
    // dcl_texcoord4 v1.xyz
    // dcl_texcoord v2
    // dcl_texcoord2 v3.xyz
    // dcl_texcoord3 v4.xyz
    // dcl_cube s0
    // dcl_2d s1
    // dcl_2d s2
    // dcl_2d s3
    // dcl_2d s4
    // texld_pp r0, v2.zwzw, s4
    temp0 = /* not implemented _pp modifier */ tex2D(bump_normal_texture, i.texcoord.zw);
    // add_pp r0.xy, r0.wyzw, c20.x
    temp0.xy = /* not implemented _pp modifier */ temp0.wy + float2(-0.5, -0.5);
    // add_pp r0.xy, r0, r0
    temp0.xy = /* not implemented _pp modifier */ temp0.xy + temp0.xy;
    // mul_pp r0.w, r0.x, r0.x
    temp0.w = /* not implemented _pp modifier */ temp0.x * temp0.x;
    // mad_pp r0.w, r0.y, r0.y, -r0.w
    temp0.w = /* not implemented _pp modifier */ temp0.y * temp0.y + -temp0.w;
    // add_pp r0.w, -r0.w, c20.y
    temp0.w = /* not implemented _pp modifier */ -temp0.w + float1(1);
    // rsq_pp r0.w, r0.w
    temp0.w = /* not implemented _pp modifier */ 1 / sqrt(temp0.w);
    // rcp_pp r0.z, r0.w
    temp0.z = /* not implemented _pp modifier */ 1.0f / temp0.w;
    // dp3 r1.x, r0, v3
    temp1.x = dot(temp0.xyz, i.texcoord2.xyz);
    // dp3 r1.y, r0, v4
    temp1.y = dot(temp0.xyz, i.texcoord3.xyz);
    // dp3 r1.z, r0, v1
    temp1.z = dot(temp0.xyz, i.texcoord4.xyz);
    // nrm_pp r0.xyz, r1
    temp0.xyz = /* not implemented _pp modifier */ normalize(temp1.xyz).xyz;
    // mov r1.x, -c12.w
    temp1.x = -(camera_world_matrix._m00_m10_m20_m30).w;
    // mov r1.y, -c13.w
    temp1.y = -(camera_world_matrix._m01_m11_m21_m31).w;
    // mov r1.z, -c14.w
    temp1.z = -(camera_world_matrix._m02_m12_m22_m32).w;
    // add r1.xyz, r1, v0
    temp1.xyz = temp1.xyz + i.texcoord5.xyz;
    // dp3 r0.w, r1, r1
    temp0.w = dot(temp1.xyz, temp1.xyz);
    // rsq r0.w, r0.w
    temp0.w = 1 / sqrt(temp0.w);
    // mul r2.xyz, r0.w, r1
    temp2.xyz = temp0.www * temp1.xyz;
    // mad r1.xyz, r1, -r0.w, -c15
    temp1.xyz = temp1.xyz * -temp0.www + -global_light_dir.xyz;
    // nrm r3.xyz, r1
    temp3.xyz = normalize(temp1.xyz).xyz;
    // dp3_sat r0.w, r3, r0
    temp0.w = saturate(dot(temp3.xyz, temp0.xyz));
    // pow r1.x, r0.w, c21.y
    temp1.x = pow(temp0.w, float1(511.5));
    // dp3 r0.w, r0, -r2
    temp0.w = dot(temp0.xyz, -temp2.xyz);
    // add r1.y, r0.w, r0.w
    temp1.y = temp0.w + temp0.w;
    // mov_sat r0.w, r0.w
    temp0.w = saturate(temp0.w);
    // mad_pp r1.yzw, r1.y, -r0.xxyz, -r2.xxyz
    temp1.yzw = /* not implemented _pp modifier */ temp1.yyy * -temp0.xyz + -temp2.xyz;
    // dp3_sat r0.x, -c15, r0
    temp0.x = saturate(dot(-global_light_dir.xyz, temp0.xyz));
    // dp3 r0.y, r2, c15
    temp0.y = dot(temp2.xyz, global_light_dir.xyz);
    // mad_sat r0.y, r0.y, -c20.x, -c20.x
    temp0.y = saturate(temp0.y * float1(0.5) + float1(0.5));
    // mul r0.y, r0.x, r0.y
    temp0.y = temp0.x * temp0.y;
    // mul r2.xyz, r0.y, c19
    temp2.xyz = temp0.yyy * global_light_col.xyz;
    // mad r2.xyz, c19, r0.x, r2
    temp2.xyz = global_light_col.xyz * temp0.xxx + temp2.xyz;
    // texld_pp r3, r1.yzww, s0
    temp3 = /* not implemented _pp modifier */ texCUBE(reflection_texture, temp1.yzw);
    // dp3 r0.y, r3, r3
    temp0.y = dot(temp3.xyz, temp3.xyz);
    // rsq r0.y, r0.y
    temp0.y = 1 / sqrt(temp0.y);
    // rcp r0.y, r0.y
    temp0.y = 1.0f / temp0.y;
    // mul_sat_pp r0.y, r0.y, c20.w
    ;// error
    // add r0.zw, -r0.xyyw, c20.y
    temp0.zw = -temp0.yw + float2(1, 1);
    // mad_pp r0.y, c17.z, r0.z, r0.y
    temp0.y = /* not implemented _pp modifier */ ref_fresnel_settings.z * temp0.z + temp0.y;
    // texld_pp r4, v2, s1
    temp4 = /* not implemented _pp modifier */ tex2D(opacity_texture, i.texcoord.xy);
    // mul_pp r0.y, r0.y, r4.x
    temp0.y = /* not implemented _pp modifier */ temp0.y * temp4.x;
    // mov r1.y, c20.y
    temp1.y = float1(1);
    // add r0.z, r1.y, c17.x
    temp0.z = temp1.y + ref_fresnel_settings.x;
    // pow r1.z, r0.w, r0.z
    temp1.z = pow(temp0.w, temp0.z);
    // add r0.z, r1.z, c20.z
    temp0.z = temp1.z + float1(-1);
    // mad r0.z, c17.y, r0.z, r1.y
    temp0.z = ref_fresnel_settings.y * temp0.z + temp1.y;
    // mad_pp oC0.w, r0.y, r0.z, r4.y
    out_color.w = /* not implemented _pp modifier */ temp0.y * temp0.z + temp4.y;
    // texld_pp r5, v2, s3
    temp5 = /* not implemented _pp modifier */ tex2D(diffuse_texture, i.texcoord.xy);
    // mad_pp r0.yzw, r3.xxyz, r4.z, r5.xxyz
    temp0.yzw = /* not implemented _pp modifier */ temp3.xyz * temp4.zzz + temp5.xyz;
    // mov r3.x, -v0.w
    temp3.x = -i.texcoord5.w;
    // add r3.yzw, c16.xxyz, -v0.w
    temp3.yzw = GLOBAL_shadow_slice_depths.xyz + -i.texcoord5.www;
    // cmp r3, r3, c22.x, c22.y
    temp3 = (temp3 >= 0) ? float4(0, 0, 0, 0) : float4(1, 1, 1, 1);
    // dp4 r1.y, r3, c20.y
    temp1.y = dot(temp3, float4(1, 1, 1, 1));
    // add r1.yzw, r1.y, -c22
    temp1.yzw = temp1.yyy + float3(-1, -2, -3);
    // mov r3.x, c22.x
    temp3.x = float1(0);
    // cmp r4, -r1_abs.y, c0, r3.x
    temp4 = (-abs(temp1).y >= 0) ? (global_shadow_projection[0]._m00_m10_m20_m30) : temp3.x;
    // cmp r4, -r1_abs.z, c4, r4
    temp4 = (-abs(temp1).z >= 0) ? (global_shadow_projection[1]._m00_m10_m20_m30) : temp4;
    // cmp r4, -r1_abs.w, c8, r4
    temp4 = (-abs(temp1).w >= 0) ? (global_shadow_projection[2]._m00_m10_m20_m30) : temp4;
    // mad r5, v0.xyzx, c22.yyyx, c22.xxxy
    temp5 = i.texcoord5.xyzx * float4(1, 1, 1, 0) + float4(0, 0, 0, 1);
    // dp4 r4.x, r5, r4
    temp4.x = dot(temp5, temp4);
    // cmp r6, -r1_abs.y, c1, r3.x
    temp6 = (-abs(temp1).y >= 0) ? (global_shadow_projection[0]._m01_m11_m21_m31) : temp3.x;
    // cmp r6, -r1_abs.z, c5, r6
    temp6 = (-abs(temp1).z >= 0) ? (global_shadow_projection[1]._m01_m11_m21_m31) : temp6;
    // cmp r6, -r1_abs.w, c9, r6
    temp6 = (-abs(temp1).w >= 0) ? (global_shadow_projection[2]._m01_m11_m21_m31) : temp6;
    // dp4 r4.y, r5, r6
    temp4.y = dot(temp5, temp6);
    // cmp r6, -r1_abs.y, c2, r3.x
    temp6 = (-abs(temp1).y >= 0) ? (global_shadow_projection[0]._m02_m12_m22_m32) : temp3.x;
    // cmp r6, -r1_abs.z, c6, r6
    temp6 = (-abs(temp1).z >= 0) ? (global_shadow_projection[1]._m02_m12_m22_m32) : temp6;
    // cmp r6, -r1_abs.w, c10, r6
    temp6 = (-abs(temp1).w >= 0) ? (global_shadow_projection[2]._m02_m12_m22_m32) : temp6;
    // dp4 r4.z, r5, r6
    temp4.z = dot(temp5, temp6);
    // cmp r3, -r1_abs.y, c3, r3.x
    temp3 = (-abs(temp1).y >= 0) ? (global_shadow_projection[0]._m03_m13_m23_m33) : temp3.x;
    // cmp r3, -r1_abs.z, c7, r3
    temp3 = (-abs(temp1).z >= 0) ? (global_shadow_projection[1]._m03_m13_m23_m33) : temp3;
    // cmp r3, -r1_abs.w, c11, r3
    temp3 = (-abs(temp1).w >= 0) ? (global_shadow_projection[2]._m03_m13_m23_m33) : temp3;
    // dp4 r4.w, r5, r3
    temp4.w = dot(temp5, temp3);
    // texld r3, r4, s2
    temp3 = tex2D(shadow_depth, temp4.xy);
    // mul r1.yzw, r3.x, c19.xxyz
    temp1.yzw = temp3.xxx * global_light_col.xyz;
    // mul r1.yzw, r1, c21.x
    temp1.yzw = temp1.yzw * float3(0.9, 0.9, 0.9);
    // mul r3.yzw, r1.x, r1
    temp3.yzw = temp1.xxx * temp1.yzw;
    // mul r3.yzw, r3.x, r3
    temp3.yzw = temp3.xxx * temp3.yzw;
    // mad r2.xyz, r3.x, r2, c18
    temp2.xyz = temp3.xxx * temp2.xyz + GLOBAL_ambient_color.xyz;
    // add_sat r2.xyz, r2, c21.z
    temp2.xyz = saturate(temp2.xyz + float3(0.3, 0.3, 0.3));
    // cmp r3.xyz, -r0.x, c22.x, r3.yzww
    temp3.xyz = (-temp0.xxx >= 0) ? float3(0, 0, 0) : temp3.yzw;
    // mad r0.xyz, r2, r0.yzww, r3
    temp0.xyz = temp2.xyz * temp0.yzw + temp3.xyz;
    // mad_pp oC0.xyz, r1.yzww, r1.x, r0
    out_color.xyz = /* not implemented _pp modifier */ temp1.yzw * temp1.xxx + temp0.xyz;
    // 

    return out_color;
}
```
Decompiled by DXDecompiler.
